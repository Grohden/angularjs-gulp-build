const gulp = require('gulp');
const chai = require('chai');
const gulpfile = require('../demo/gulpfile.babel');
chai.use(require('chai-fs'));

describe('Views task class', function() {
    it('should copy the files to the specified folders', function() {
        // gulp.task('demo:views')();
    });
});